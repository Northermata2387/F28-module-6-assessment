import { run } from "jest"

const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    // CODE HERE

    // test('check that shuffleArray returns an array', () => {
    //     expect(shuffleArray.shuffleArray().toBe([]))
    // })

    // test('check that it returns an array of the same length as the argument sent in', () => {
    //     expect(shuffleArray.shuffleArray().toBe([arrCopy.length]))
    // })

})

// Make sure to run npm i chromedriver@latest